{
  "name": "week3",
  "uiEditable": true,
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "5.0.3"
  },
  "devDependencies": {
    "twilio-run": "^4.1.0"
  },
  "engines": {
    "node": "18"
  }
}
